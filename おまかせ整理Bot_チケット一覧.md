# おまかせ整理Bot チケット一覧

仕様書の機能要件（6章）をベースにEpic単位で分解。各チケットに受け入れ条件（AC）を記載。
優先度はP0（MVP必須）〜P3（余裕があれば）。

---

## Epic 1: 監視・検出（P0）

### T1. フォルダ監視機能の実装
**概要**：ダウンロードフォルダを常時監視し、新規ファイル作成時にパイプラインを起動する。
**優先度**：P0

**受け入れ条件**
- [ ] 指定フォルダに新規ファイルが作成されると検出イベントが発火する
- [ ] 対象外拡張子のファイルではイベントが発火しない
- [ ] アプリ起動中は監視が継続し、途中で停止しない

---

## Epic 2: スクリーニング（P0）

### T2. 拡張子判定・不要ファイル除外
**概要**：監視で検出したファイルが処理対象かどうかを判定する。
**優先度**：P0

**受け入れ条件**
- [ ] txt / pdf / jpg / png 等の対象拡張子のみが後続処理に渡される
- [ ] `.tmp`, `.crdownload` 等の一時ファイルは処理対象から除外される
- [ ] 読み込みに失敗したファイルはエラーログを出力し、処理をスキップする

---

## Epic 3: AI分類（P0）

### T3. コンテンツ抽出機能
**概要**：ファイル種別ごとに内容をテキスト化する。
**優先度**：P0

**受け入れ条件**
- [ ] テキストファイルの中身を文字列として取得できる
- [ ] PDFから`pdfplumber`でテキストを抽出できる
- [ ] PDF抽出結果が空（スキャンPDF）の場合、`pdf2image`＋`pytesseract`でOCRにフォールバックする
- [ ] 写真はGemini API（画像入力）に渡し、OCR＋キャプションを兼ねたテキストを1回の呼び出しで取得できる

### T4. Gemini APIによる分類呼び出し
**概要**：抽出コンテンツと既存カテゴリ一覧をGemini APIに渡し、category/subtags/summaryを取得する。
**優先度**：P0

**受け入れ条件**
- [ ] 既存カテゴリ一覧を含めたプロンプトでAPIを呼び出せる
- [ ] レスポンスから`category` / `subtags` / `summary`をJSONとしてパースできる
- [ ] レスポンスがJSON形式でない場合、パースエラーとして検知しログに残せる
- [ ] API呼び出し失敗時にリトライ、またはエラーハンドリングができる

### T5. カテゴリ重複防止ロジック
**概要**：新規カテゴリ名を既存カテゴリとembedding類似度で照合し、表記ゆれを統合する。
**優先度**：P0

**受け入れ条件**
- [ ] 提案されたカテゴリ名をembedding化できる
- [ ] 既存カテゴリとのコサイン類似度を計算できる
- [ ] 類似度が閾値（暫定0.85）以上の場合、既存カテゴリ名に置き換えられる
- [ ] 類似度が閾値未満の場合、新規カテゴリとしてDBに登録される

---

## Epic 4: ファイル移動・DB保存（P0）

### T6. ファイル移動＋メタデータ保存
**概要**：分類結果に基づき実ファイルを移動し、DBにメタデータを保存する。
**優先度**：P0

**受け入れ条件**
- [ ] カテゴリ名のフォルダが存在しない場合は自動作成される
- [ ] ファイルが対象フォルダへ移動される
- [ ] 移動前の元パスが`trash_log`に記録される
- [ ] `files`テーブルに全メタデータ（path, filetype, category, subtags, summary, embedding, hash, size, created_at）が漏れなく保存される

---

## Epic 5: 検索UI（P1）

### T7. 意味検索バーの実装
**概要**：自然文入力による類似検索とカード表示を実装する。
**優先度**：P1

**受け入れ条件**
- [ ] 検索バーに自然文を入力すると結果がカード形式で表示される
- [ ] クエリのembeddingとファイルのembeddingのコサイン類似度で上位N件が返る
- [ ] 該当ファイルがない場合「見つかりませんでした」等のメッセージが表示される
- [ ] カードにファイル名・カテゴリ・要約・サムネイルが表示される

---

## Epic 6: 放置ファイル検出（P2）

### T8. 放置ファイル検出（片付けタイム）
**概要**：片付けタイムを始めた際にその場で候補を算出し、会話ログで1件ずつ「残す」「ごみ箱へ」を選ばせる。詳細は `Document/T8_放置ファイル検出_仕様書.md`。
**優先度**：P2

**受け入れ条件**
- [ ] 候補があるとき、コハクが吹き出しで片付けタイムに誘い、同じ内容が会話ログにも残る
- [ ] 片付けタイムを始めると、アクティブファイルを対象にその場で候補が算出される
- [ ] 30日以上開いていないファイルと、重複（ハッシュ一致）の残す側でないファイルが候補になる
- [ ] 未アクセス期間は`last_accessed_at`で判定される（分類時に`created_at`で初期化済み）
- [ ] 候補を1件ずつ「残す」「ごみ箱へ」で選べる。「残す」は30日間再提案されない

---

## Epic 7: ゴミ箱/Undo（P3）

### T9. ゴミ箱・Undo機能
**概要**：削除・移動操作を取り消せる安全策を実装する。
**優先度**：P3

**受け入れ条件**
- [ ] 削除操作は即時削除ではなく`status=trashed`への変更として扱われる
- [ ] 直近の移動/削除操作をUndoで元に戻せる
- [ ] 操作内容が`trash_log`に記録される
