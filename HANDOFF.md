# 引き継ぎメモ（作業を再開する人・次のチャット向け）

最終更新: 2026-09-17 / 対象ブランチ: `sub`

**作業は `sub` で進めます。このファイルは `main` には入れません**（`main` はチームで共有する動く版）。

### ブランチの状態

| ブランチ | 中身 |
|---|---|
| `main` | T1〜T8＋UI。`sub` を取り込んだ後、HANDOFF.md だけ削除してある |
| `sub` | `main` と同じ中身＋HANDOFF.md |
| `t8` | T8の作業ブランチ。`sub` にマージ済みなので使わない |

`sub` を `main` に反映するときは HANDOFF.md を入れないこと。`main` 側で削除済みなので、
`sub` で HANDOFF.md を書き換えた後にマージすると「変更と削除」の衝突になる。削除を選んで解決する。
逆に `main` を `sub` に取り込むと HANDOFF.md が消えるので、その場合は復元する。

Claude Codeの会話履歴はマシンやチャットをまたいで引き継がれません。
前のやりとりで決まったことは、ここに書いてあることが全てです。

---

## 0. 再開したら最初に読むところ

- **プロジェクトは終了（2026-09-17 デモ発表済み）**。発表資料は `おまかせ整理Bot_明るいデザイン完成_修正版.pptx`（最終版）と
  その前の版 `おまかせ整理Bot_明るいデザイン完成.pptx`。`sub` の内容は `main` にマージ済み（HANDOFF.md を除く）

- **いまの到達点**: T1〜T8とデスクトップ常駐UI（MOFU）が動いています。
  `python main.py` で起動し、ダウンロード → 分類 → 通知 → 訂正 → 検索 → 開く → 片付け まで一通り通ります
- **T8は実機確認済みで `sub` と `main` にマージ済み**。仕様は `Document/T8_放置ファイル検出_仕様書.md`
- **T9（ゴミ箱/Undo）も `sub` にマージ済み**（伊藤さん作の `t9` ブランチに修正を加えたもの）。全チケット実装済み
- **次にやること**: 実機での T9 確認（5章(B)）と、`main` への反映
- **UIは白黒ドット絵で確定**。3章の約束事を守ってください。一度「見づらいだけ」と言われて作り直した経緯があります
- **GUIは PySide6**。PyQt6 はこのPCでは起動すらしません（7章）

---

## 1. セットアップ

```bash
git clone https://github.com/t-kume-dev/hackson2026
cd hackson2026
git checkout sub          # main には HANDOFF が入っていない
pip install -r requirements.txt
python main.py            # ~/Downloads を監視してMOFUが左下に出る
```

Python 3.12（3.12.3）と 3.13（3.13.14）で動作確認済み。
`requirements.txt` は全モジュールのimportを満たしています。
torch は sentence-transformers が依存として引いてくるので明記していません。

### gitに入っていないので手で用意するもの

| 対象 | 内容 |
|---|---|
| `.env` | `OPENAI_API_KEY=<キー>` の1行（有料キー。ユーザー本人のものではない）。**無いと分類が全部失敗します**（UIは起動する）。`GEMINI_API_KEY` は使っていない |
| `omakase.db` | 実行すれば自動で作られる |
| `organized/` | 同上。分類されたファイルの置き場 |

ドット絵フォント `assets/fonts/DotGothic16-Regular.ttf`（SIL OFL、ライセンス文同梱）は
gitに入っています。

### 初回起動が遅い点

検索用の埋め込みモデル（e5-base、約1.1GB）が初回にHuggingFaceからダウンロードされます
（`~/.cache/huggingface/`）。**デモの前に一度起動してキャッシュを作っておくこと。**
常駐中のメモリはおよそ500MBで、大半がこのモデルです。

### 任意（無くても動く）

Poppler（`pdf2image`用）とTesseract-OCR（`pytesseract`用）は外部バイナリで、未インストールです。
効くのは「文字を含まないスキャンPDF」のOCRだけで、普通のPDF・画像・テキストには影響しません。

---

## 2. いまどこまで出来ているか

```
T1 t1_watch.watch_folder         フォルダ監視（watchdog、新規ファイルのみ検知）
T2 t2_screening.screen_file      処理対象かの判定＋種別(text/pdf/photo)
T3 t3_content.extract_content    中身の抽出（画像はbytesのままT4へ）
T4 t4_classify.classify_content  Gemini APIで カテゴリ/サブタグ/要約 を生成
T5 t5_dedupe.resolve_category    カテゴリを階層ごとに既存の表記へ揃える（表記の正規化のみ）
T6 t6_filemanager.save_result    カテゴリフォルダへ移動＋DB保存
   t6_filemanager.recategorize   分類の訂正（別カテゴリへ移し直す）
T7 t7_search.search              自然文で意味検索
T8 t8_cleanup.find_candidates    片付けの候補（30日開いていない / 重複）を算出
   t8_cleanup.keep_file          「残す」。30日間は候補に出さない
   t8_cleanup.trash_file         「ごみ箱へ」。organized/.trash/ へ退避して status=trashed
   pipeline.process_file         T1〜T6の受け渡し（UI版とコンソール版で共有）
   ui_kohaku                     常駐キャラ＋チャットパネル
T9 t9_undo.undo_last_action     直近の移し替え/ごみ箱を1件戻す（file_id指定でそのファイルに限る）
   main.py                       入口。既定でUIを起動、--console でログのみ、--undo で直近を1件戻す
```

### 起動方法

```bash
python main.py                      # ~/Downloads を監視してUI起動
python main.py <フォルダ>            # 監視対象を指定
python main.py <フォルダ> --console  # UIなし。どの段で止まったか見るデバッグ用
```

ウィンドウは出ず、デスクトップ左下にキャラだけが常駐します。
左クリックで会話パネル、ドラッグで移動、右クリックでメニュー（MOFUと話す / 片付けタイム / 終了）。タスクトレイにも居ます。

### DBはPCごとに別物

`omakase.db` と `organized/` はgitに入らないので、**別のPCでは空のDBから始まります。**
前のPC（作業を始めたPC）には、同じ画像を3回入れた実データが4件ありましたが、移動先のPCにはありません。
T8の確認用データの作り方は5章(A)に書いてあります。

---

## 3. UIの方針（ユーザーと相談して確定したもの）

### 構成

デスクトップ左下に常駐する相棒キャラ **MOFU** が、2つの役割を持ちます。

- **自発**: ダウンロードを検出したら吹き出しで「どこに入れたか」を知らせる（**5秒で消える**）
- **ユーザー始動**: クリックで会話パネルが開き、自然文でファイルを探せる

検索結果も分類通知も訂正も、**すべて1本の会話ログに流します。**
T8の片付けタイムもこのログに流しています。T9のUndoも同じ形にする想定です。
タブで画面を分ける構成は採りません。

### 決まっていること

- **ファイルビューアは作らない。** 「開く」はOSの既定アプリに投げるだけ
  （エクスプローラーでダブルクリックしたのと同じ）。開いたら会話パネルは閉じ、キャラだけ残す
- **エクスプローラーの代替は作らない。** ツリー表示もリネームも範囲外
- **分類の訂正は必須。** 入口は2つ。通知の「違うカテゴリ」と、会話ログのカードの
  「別のカテゴリへ」（通知は5秒で消えるので、カード側が無いと後から直せない）。
  候補は中身の近いファイルが入っているカテゴリを近い順に出し、最後に必ず「新しく作る」を付ける
- **カードのボタンは「開く / 別のカテゴリへ / 場所を表示」の3つ。**
  「似たファイル」は外した（`t7_search.find_similar()` は残っているので戻すのは簡単）
- **キャラは常に最前面。** 既定アプリが前面を奪うと裏に隠れるため、定期的に押し上げている
- **会話は新しい発言が来たら最下部へ自動で追従**（ChatGPTと同じ。自分で上にスクロール中は追従しない）

### 見た目：白黒のドット絵

最初に作ったオレンジ・角丸のモックから、ユーザーの指示で白黒ドット絵に変更しました。

- 吹き出しは**黒地・白枠・白文字**（ユーザー指定）
- 色は使わず、強調は白黒反転で表す。メッセージに `<b>` を書くと自動で反転の帯になる
- 角丸・影・グラデーションは使わない
- **白枠で囲むのはMOFUの吹き出しだけ。** カードは枠なしで面の明るさで区切り、
  「開く」以外のボタンは細い灰色枠。全部を白枠にしたら「見づらくなっただけ」と言われた
- **フォントは2系統。** MOFUの声（吹き出し・ボタン・タイトル）はドット絵フォント
  DotGothic16 を **14px以上** で使う。ファイル名・要約など情報の多い部分は普通のUIフォント。
  DotGothic16は16px基準で、小さくすると潰れて読めない。太字は滲むので使わない
- 真っ黒・真っ白ではなく `#111111` / `#f4f4f4`

### キャラクター

丸くて下ぶくれ、ほっぺと小さな足のある、口の大きなドット絵のキャラ（ユーザーの評価は「めちゃくちゃ良くなった」）。

| 状態 | 動き |
|---|---|
| 普段 | 「ω」の口。呼吸で1ドット上下し、時々まばたきする |
| 分類中 | 「> <」の目で口をあんぐり開け、右から飛んでくるファイルを食べる → 飲み込む → もぐもぐ、を繰り返す |
| 分類完了 | 「^ ^」の顔で少し弾む |

コマは `ui_kohaku.py` の `SPRITE_IDLE` / `SPRITE_BLINK` / `SPRITE_HAPPY` / `SPRITE_OPEN` /
`SPRITE_WIDE` / `SPRITE_FILE` に文字列のドット絵で書いてあります
（`#` 黒 / `o` 白 / `g` 灰 / `.` 透明、18×16ドット）。形を変えるときはここを書き換えるだけです。

最初の方向性を相談したときのモック（見た目は古い）: https://claude.ai/artifact/EoA4bCxAxf9P315zsk4pd2

---

## 4. DB構成

**SQLiteアクセスは全て `db.py` に集約しています。** 新しいチケットでも
`sqlite3.connect` を直接呼ばず、`db.py` に関数を足してください。

- **DBファイルのパスは `db.DB_PATH`（= `omakase.db`）ただ1つが正。**
  相対パスを書くと実行場所次第で別のDBが作られ、データが消えたように見えます
- テーブルは仕様書8章どおり `files` / `categories` / `trash_log` の3つ
- `categories.embedding` は現在使っていない（T5がembeddingをやめたため常にNULL）
- T6はfilesとtrash_logのINSERTを1トランザクションにまとめています
- `files.last_accessed_at` は**分類した時点**で `created_at` と同じ値が入り、
  以降は**アプリ経由で開いたとき**に更新されます（`t7_search.open_file()`）。
  T8の「未アクセス期間」はこの列だけを見ればよい。`last_accessed_at == created_at` なら一度も開かれていない（仕様書6.5）

T7で足した関数: `get_active_files()` / `get_file()` / `touch_file()` /
`update_file_location()` / `set_file_status()`（T9で使う想定）

T8で足したもの: `files.kept_at` 列（「残す」を押した日時。古いDBには `_migrate()` で追加）、
`set_kept()` / `mark_trashed()`（status・パスの更新と `trash_log` の `delete` 記録を1トランザクションで行う）

### embedding

| 保存先 | モデル | 次元 | 用途 |
|---|---|---|---|
| `files.embedding` | `intfloat/multilingual-e5-base`（ローカル） | 768 | T7の検索、訂正候補の並べ替え |

`files.embedding` と比べるベクトルは、必ず `t6_filemanager.embed_query()` で作ってください。
e5系に必須のプレフィックス（`passage:` / `query:`）と正規化をこの中で揃えています。
別の方法で作ると、例外は出ずに検索結果だけがデタラメになります。

---

## 5. 次にやること

### デモの準備
- **デモ当日に** `python demo/setup_demo.py` を実行する（アプリは終了しておく）。DBと `organized/` を消し、
  以前から使っていた想定の15件を OpenAI で分類して登録し、日付を過去にずらす。本番で入れるファイルは `demo/drop/` にできる
- 筋書き: `demo/drop/` の3件（契約書PDF・領収書画像・出張精算）＋zipを `~/Downloads` へ入れる → 分類 →
  「昨日のPDF」「昨日の写真」で期間と種類の絞り込み → 「先週の契約書」は見つからない（4〜11日前には何も置いていない）→
  右クリックの「片付けタイム」（重複2件・30日以上開いていない5件）
- 起動から1分後に片付けタイムの声かけが自動で出るので、話す順番に注意する

### (A) T8 放置ファイル検出【完了】

仕様は **`Document/T8_放置ファイル検出_仕様書.md`** にまとめ、ユーザーと相談して確定しています。要点:

- 候補は「**30日以上開いていない**」か「**重複していて残す側ではない**」のどちらか。サイズは条件にしない
- 重複の残す側は、最近開いたもの → 先に入れたもの の順で決める
- MOFUが吹き出しで「片付けタイムにしない？」と誘い、**同じ内容を会話ログにも残す**。
  起動1分後に1回、その後は1日1回まで。通知中・分類中は待つ。右クリックメニューからも始められる
- 候補は1件ずつカードで出し、**「残す」「ごみ箱へ」の二択**。1回5件まで。パネルを閉じたらその回は終わり
- 「残す」は30日間再提案しない。「ごみ箱へ」は `organized/.trash/<id>_<名前>` へ退避

検証済みの範囲（前のPCで実施）:
- 一時的なDBで、30日以上開いていない / 重複の残す側の決め方 / 残してから30日以内・30日を過ぎたもの /
  実ファイルが消えたもの / ごみ箱へ入れる処理（二重に入れようとした場合を含む）が仕様どおりに動いた
- 実データのコピーを使い、画面に出さない形（offscreen）で、声かけ → はじめる → 残す → ごみ箱へ → 候補0件 まで動き、
  パネルの見た目もスクリーンショットで確認した
- 別のPCでユーザーが実際の画面で確認し、問題なし

#### 別のPCでの確認手順

1. `git checkout t8` → `python main.py` で起動（既存のDBがあれば `kept_at` 列が自動で追加される）
2. **重複の確認用データを作る**: 同じ画像ファイルを、ダウンロードフォルダに3回入れる
   （1回入れて分類されたら、同じファイルをもう一度コピーして入れる。を繰り返す）。
   そのうち1件を、会話パネルで検索して「開く」から開いておく（それが「残す側」になる）
3. 仕様書9章の手順で確認する。候補は開いていない2件になる
   - **確認すると実ファイルが1件 `organized/.trash/` に移る**
   - 声かけは起動から1分後。すでに声をかけた後なら、右クリックの「片付けタイム」から始められる
4. **30日の条件の確認用データ**: ユーザーが用意する方針（アプリ側にデモ用の仕掛けは作らない）。
   手早く作るなら、分類済みのファイルの日時をDBで過去にずらす。例（id=2 を45日前にする）:
   ```python
   import sqlite3, datetime
   old = (datetime.datetime.now() - datetime.timedelta(days=45)).isoformat()
   c = sqlite3.connect("omakase.db")
   c.execute("UPDATE files SET created_at=?, last_accessed_at=? WHERE id=?", (old, old, 2))
   c.commit()
   ```
   アプリを起動したままでも効く（候補は片付けタイムを始めるたびに算出し直すため）
5. 問題が無ければ `t8` を `sub` にマージし、HANDOFF の0章・ブランチの表を更新する

### (B) T9 ゴミ箱/Undo【実装済み・実機確認待ち】

- 入口は3つ: カードの「ごみ箱へ」の後に出る「元に戻す」、「別のカテゴリへ」で移した後の「元に戻す」、
  キャラ/トレイの右クリックメニューの「元に戻す」（直近の1件）
- カードの下のボタンは「開く / 別のカテゴリへ / 場所を表示」の3つのまま。「ごみ箱へ」はファイル名の右に小さなリンクとして置いた
  （4つ並べるとパネル幅400pxからはみ出したため。「開く」の隣に消す操作を置かない意図もある）。中身は `t8.trash_file()` と同じ
- 取り消せるのは organized/ の中での移し替えとごみ箱行きだけ。**分類時の移動（ダウンロードフォルダから）は戻さない**
  （戻すと監視に再検出されて分類し直されるため）。`trash_log` の元のパスが organized/ の中かで見分ける
- カテゴリは元のパスのフォルダ階層から復元する。フォルダ名は使えない文字を除いてあるので、
  カテゴリ名に `:` などが含まれていた場合は、戻した後の表記がわずかに変わる
- 取り消した `trash_log` の行は消す。同じファイルを続けて戻すと、さらに1つ前の操作が戻る
- マージ時に直したこと: `_pick_category` のインデント崩れ（押すと落ちた）、カテゴリ復元が1階層だけだった点、
  分類直後に「元に戻す」を押すとダウンロードフォルダへ戻っていた点、「元に戻す」が間に入った別ファイルの操作を戻していた点、
  SQLの直書き（`db.get_trash_logs()` / `db.restore_file()` に移した）

### (C) 6章のテスト項目

T8以前の項目（6章の1〜23）は、ユーザーが一通り確認済み。
不合格だったのは6番（キャラの最前面）だけで、Windows標準のフォトを全画面にするとキャラが裏に回ったまま戻らなかった。
これは修正済みで、ユーザーの確認で「改善した」と報告を受けている（7章「最前面指定だけでは〜」）。

### (D) 未決事項

- **訂正の学習**: 訂正はその場限りで、同種のファイルが来るとまた同じ間違いをする（仕様書12.2）
- **`epic1.py` と `test_t2_read_failure.py`** が配線から外れたまま残っている。現役か要確認
- **グローバルホットキー**（Ctrl+Space等でどこからでも呼ぶ）は未実装。Qtには標準の手段が無い

### (E) 今後の改善案

仕様書 **12章** にまとめてあります（ローカルLLMへの置き換え、訂正の学習、配布時のサイズと署名、検索の規模）。
ハッカソンのスコープ外という扱いで合意済みです。

---

## 6. 動作確認の手順

上から順にやると一連の流れになります。

**起動**
1. `python main.py` → ウィンドウは出ず、左下にキャラ。タスクトレイにもアイコン
2. キャラをドラッグ → 移動できる。離しても会話パネルは開かない
3. 右クリック → 「MOFUと話す」「片付けタイム」「終了」
4. 左クリック → 会話パネルが開き、キャラは消える。「おかえり」＋候補チップ
5. × か Esc → パネルが閉じてキャラが戻る
6. 他のアプリを前面にしても、キャラは最前面に居続ける（Windowsのフォトを全画面にしても）

**ダウンロード → 分類**
7. 監視フォルダにPDFを入れる → キャラが口を開けてファイルを食べる → 吹き出しが出る
8. 吹き出しは5秒で消える
9. 実ファイルが `organized/<カテゴリ>/` に移動している
10. テキスト・写真でも同じ流れ。写真は要約に画像の中身が書かれているか
11. 対象外（.zip等）は何も起きない

**訂正**
12. 吹き出しの「違うカテゴリ」→ パネルが開き、候補＋「新しく作る」
13. 候補を選ぶ → 移動して「〇〇に移したよ」＋カード
14. 「新しく作る」→ 名前を入れると新カテゴリができて移動する。空でキャンセルなら何もしない
15. 吹き出しが消えた後、カードの「別のカテゴリへ」からも同じことができる

**検索**
16. 普通の言葉（「契約書」「統計の資料」）→ カードが出る。初回だけ数秒かかる
17. 関係ない言葉 → 「見つからなかった」と返る（無理に何か出したら不合格）
18. 「今日入れたやつ」→ `[期間: 今日]` で一覧が出る
19. 「PDF全部」→ `[種別: PDF]` で絞られる
20. カードの「開く」→ 既定アプリで開き、パネルは閉じ、キャラは最前面に残る
21. カードの「場所を表示」→ エクスプローラーでそのファイルが選択された状態になる
22. 新しい発言が来たら自動で最下部までスクロールする

**終了**
23. 右クリック → 終了 → プロセスが消える

**片付けタイム（T8）** は `Document/T8_放置ファイル検出_仕様書.md` の9章と、5章(A)の手順を参照。

**注意**: 「先週の〇〇」は、今日入れたファイルしか無いうちは何も出ません（仕様どおり）。
手で `organized/` のファイルを動かしてもDBは追従しません。

---

## 7. 踏んだ罠

- **AIはOpenAIに移行した（Geminiには戻さない）**: Geminiの無料枠は分類が1日20回で、テスト中に尽きた。
  T4は `.env` の `OPENAI_API_KEY` を見て OpenAI（既定 `gpt-5.5`、`OPENAI_MODEL` で変更可）を使う。
  26件の自作データで gpt-5-mini / gpt-5.4-mini / gpt-4.1-mini / gpt-5.4 と比べ、gpt-5.5 が最も妥当だった。
  miniモデルは既存カテゴリへ無関係なファイルを押し込む（論文→統計学、議事録→旅行）ので使わない
- **T5のembedding統合は誤統合していた**: カテゴリをフルパス（「契約書／雇用契約書」）でembeddingすると、
  共通の親に引っ張られて別物同士が0.93〜0.98になり、雇用契約書→賃貸借契約書、重回帰→単回帰と誤って統合した。
  末尾の名前だけにしても「単回帰/重回帰 0.945」と「写真/画像 0.946」が重なり、しきい値で分けられない。
  そこでT5は表記の正規化（全角半角・大小・空白）だけにし、意味の統合はT4のプロンプト（既存の表記をそのまま使う）に任せた

- **PyQt6はこのPCで使えない**: Windowsのアプリケーション制御ポリシーがDLLをブロックし、importすら通らない。
  Qt公式の署名付きバイナリである **PySide6** を使っている。違いはシグナル定義が `Signal` になる点だけ。戻さないこと
- **最前面指定だけではキャラが隠れる**: `WindowStaysOnTopHint` を付けても、既定アプリが起動すると前面を奪われる。
  特にWindowsのフォトを全画面にすると、Qtの指定だけでは勝てない。
  Windowsでは `SetWindowPos` を `SWP_NOACTIVATE` 付きで1.5秒ごとに呼んでいる（フォーカスは奪わない）。
  `raise_()` はフォーカスを奪うことがあるので使わない
- **ctypesは argtypes を宣言しないと黙って失敗する**: 以前の `SetWindowPos` 呼び出しは型を宣言しておらず、
  `HWND_TOPMOST`（-1）が64bitで正しく渡らないため、**一度も成功していなかった**（戻り値0、エラー1400）。
  例外が出ないので気づけず、Chromeでは Qt の指定だけで足りていたため「効いている」ように見えていた。
  Win32 APIを呼ぶときは `argtypes` / `restype` を必ず宣言し、戻り値を確認すること
- **`last_accessed_at` は分類時に入れる**: 以前は開いたときだけ記録していたため、一度も開いていないファイルは空だった。
  今は分類時に `created_at` と同じ値を入れ、古い行は `db._migrate()` で埋めている。
  `created_at` は消さないこと（T7の「今日入れたやつ」などの期間検索と、「一度も開いていない」の判定に使う）
- **Qtのスタイル継承**: セレクタ無しで `background:` を書くと子のラベルにまで効き、カードの上に黒い帯が出た。
  `QWidget#名前 { ... }` のように自分だけに効かせること
- **折り返しラベルは縮む**: `setWordWrap(True)` のQLabelは余裕があっても最小幅まで縮み、変な位置で改行する。
  吹き出しは一番長い行の幅を測って最小幅を確保している
- **「今日入れたやつ」が0件だった**: 期間を抜いた残りの「入れたやつ」を意味検索にかけ、何にも似ていない判定になっていた。
  中身を指さない言い回し（`t7_search.FILLER`）を落とし、何も残らなければ一覧として返すようにした
- **検索のしきい値**: e5は無関係な文でも類似度0.78前後を出す。`MIN_SCORE = 0.82` と、1位からの相対足切り
  `RELATIVE_MARGIN = 0.06` の二段で切っている。どちらも少数データでの暫定値
- **期間は暦の日付で数える**: 以前は24時間単位で幅も広く、「今日」に昨日のファイルが混ざっていた。今日・昨日・一昨日は1日ぴったり
- **「先週」は4〜11日前**: 暦週で切ると、水曜に「先週の資料」と言われたときに8日前を取りこぼすため
- **カレントディレクトリ依存**: 以前はDBと `organized` が相対パスで、実行場所次第で別のDBができた。今は全てモジュール基準
- **テスト副産物の誤コミット**: `*.db` と `organized/` は `.gitignore` 済み。`git add -A` の前に `git status` を見ること
- **壊れたgitlink**: ブランチ `t3pair` / `t4_otake` / `t5_otakeeee` / `t6` には入れ子リポジトリ `hackson2026` が残っている。
  `sub` では削除済みなので、これらを再マージしないこと
- **ベンチマークは規模が命**: embeddingモデルの比較を少数の文書でやると全モデルが満点になる。
  MiniLMとe5-baseの差（83.3%と94.4%）は300件にして初めて出た
