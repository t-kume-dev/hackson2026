# おまかせ整理Bot チケット別 入力/出力一覧

各チケットが「何を受け取り、何を渡すか」だけをまとめたもの。
中身の実装方法（どのライブラリを使うか、どう書くか）は各ペアの裁量に任せる。
これはモブプロで最初に合意するための、大まかな役割分担表。

| チケット | 受け取るもの（入力） | 渡すもの（出力） |
|---|---|---|
| T1. フォルダ監視 | 監視対象のフォルダパス | 新規ファイルのパス |
| T2. スクリーニング＋種別判定 | ファイルパス | 「処理していいか」の判定＋ファイル種別（text/pdf/photo） |
| T3. コンテンツ抽出（text/pdf/photo） | ファイルパス | 中身のテキスト |
| T4. AI分類（Claude API呼び出し） | 抽出済みテキスト＋既存カテゴリ一覧 | カテゴリ名・サブタグ・要約 |
| T5. カテゴリ重複防止 | 新しいカテゴリ名＋既存カテゴリ一覧 | 最終的に使うカテゴリ名（既存流用 or 新規） |
| T6. ファイル移動＋DB保存 | ファイルパス＋分類結果一式 | 移動後のパス＋DB保存完了 |
| T7. 意味検索UI | 検索したい自然文 | 該当ファイルのカード一覧 |
| T8. 放置ファイル一覧 | （特になし、DBを直接参照） | 整理候補ファイルの一覧 |
| T9. ゴミ箱/Undo | 対象ファイルID／直前の操作 | ゴミ箱への移動 or 元に戻す |

---

## 補足：SMが書く「配線」部分

T1〜T6は直列につながっているので、誰かが「前のチケットの出力を次のチケットに渡す」処理を書く必要がある。
ここだけはあなた（SM）が担当し、後輩は自分のチケットの入力と出力さえ守れば中身は自由、という状態を作る。
